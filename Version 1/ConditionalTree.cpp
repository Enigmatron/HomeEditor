//
//  ConditionalTree.cpp
//  HomeEditor
//
//  Created by Idries Kysia on 2/25/18.
//  Copyright © 2018 Idries Kysia. All rights reserved.
//

#include "ConditionalTree.hpp"

/*
void ConditionalManager::addToPair(int index, int iteration, IConditional cond){
    //    IConditional* co;
    //    if(index <0 || index > flow)
    //    {
    //        index = sizeof(flow)-1;
    //        co* =
    //    }
    //    for(int i =0; i < iteration; i++){
    //        if()
    //    }
}

int* ConditionalManager::update(double in){
    int temp[] = new int[sizeof(flow)];
    int j = 0;
    for(int i = 0; i < sizeof(flow);i++){
        if(flow[i].condition.Condition(in))
        {
            temp[j] = flow[i].messageIndex;
            j++;
        }
    }
    return temp;
}

void ConditionalManager::addPair(CondPair pair){
    CondPair* temp = new CondPair[sizeof(flow)*2];
    
    if(flow[sizeof(flow)-1]!=NULL){
        for(int i = 0; i < sizeof(flow) ; i++){
            temp[i] = flow[i];
        }
        temp[sizeof(flow)] = pair;
        delete [] flow;
        flow = temp;
    }
    else{
        for(int i = 0; i<sizeof(flow);i++){
            if(flow[i] == NULL){
                flow[i] = pair;
                return void;
            }
        }
    }
}

void ConditionalManager::removePair(int index){
    int sub = 0;
    if(!(sizeof(flow)-1 >= index)){
        return void;
    }
    for(int i = index+1; i < sizeof(flow)-1; i++){
        flow[i-1] = flow[i];
        
    }
}

ConditionalManager::ConditionalManager(char* list) {
    
}

ConditionalManager::ConditionalManager(const ConditionalManager& orig) {
}

ConditionalManager::~ConditionalManager() {
}
 */
